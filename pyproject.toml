[project]
name = "ytbot"
version = "0.1.0"
dependencies = [
    "yt-dlp",
    "requests",
    "pymongo",
    "python-dotenv",
    "pyTelegramBotAPI",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
